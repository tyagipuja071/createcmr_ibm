/*
This file was automatically generated by the EntityCreator engine on ${currdate}.
*/
package com.ibm.cio.cmr.request.entity;

import java.io.Serializable;
#if (${table.hasTimestamp()})
import java.util.Date;
#end

import javax.persistence.Column;
import javax.persistence.EmbeddedId;
import javax.persistence.Entity;
import javax.persistence.Table;
#if (${table.hasTimestamp()})
import javax.persistence.Temporal;
import javax.persistence.TemporalType;
#end

/**
 * The persistent class for the ${tableName} database table.
 * 
 * @author ${user}
 */
@Entity
@Table(name = "${tableName}", schema = "${schema}")
public class ${tableNameJava} extends BaseEntity<${tableNameJava}PK> implements Serializable {
  private static final long serialVersionUID = 1L;

  @EmbeddedId
  private ${tableNameJava}PK id;

  public ${tableNameJava}PK getId() {
    return id;
  }

  public void setId(${tableNameJava}PK id) {
    this.id = id;
  }
  
${columns}

}