/*
This file was automatically generated by the EntityCreator engine on Tue Sep 06 17:00:41 CST 2016.
 */
package com.ibm.cio.cmr.request.entity;

import java.io.Serializable;

import javax.persistence.Column;
import javax.persistence.Embeddable;

import org.apache.commons.lang.StringUtils;

/**
 * The primary key class for the USER_ROLES database table.
 * 
 * @author Jeffrey Zamora
 */
@Embeddable
public class UserRolesPK extends BaseEntityPk implements Serializable {

  // default serial version id, required for serializable classes.
  private static final long serialVersionUID = 1L;

  @Column(name = "USER_ID")
  private String userId;

  @Column(name = "ROLE_ID")
  private String roleId;

  @Column(name = "SUB_ROLE_ID")
  private String subRoleId;

  public String getUserId() {
    return this.userId;
  }

  public void setUserId(String userId) {
    this.userId = userId;
  }

  public String getRoleId() {
    return this.roleId;
  }

  public void setRoleId(String roleId) {
    this.roleId = roleId;
  }

  public String getSubRoleId() {
    return this.subRoleId;
  }

  public void setSubRoleId(String subRoleId) {
    this.subRoleId = subRoleId;
  }

  @Override
  public boolean equals(Object other) {
    if (this == other) {
      return true;
    }
    if (!(other instanceof UserRolesPK)) {
      return false;
    }
    UserRolesPK o = (UserRolesPK) other;
    return this.userId.equals(o.userId) && this.roleId.equals(o.roleId) && this.subRoleId.equals(o.subRoleId);

  }

  @Override
  public int hashCode() {
    final int prime = 31;
    int hash = 17;
    hash = hash * prime + (this.userId != null ? this.userId.hashCode() : 0);
    hash = hash * prime + (this.roleId != null ? this.roleId.hashCode() : 0);
    hash = hash * prime + (this.subRoleId != null ? this.subRoleId.hashCode() : 0);

    return hash;
  }

  @Override
  protected boolean allKeysAssigned() {
    return !StringUtils.isEmpty(this.userId) && !StringUtils.isEmpty(this.roleId) && !StringUtils.isEmpty(this.subRoleId);

  }
}