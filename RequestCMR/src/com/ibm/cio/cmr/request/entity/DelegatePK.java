/*
This file was automatically generated by the EntityCreator engine on Thu Aug 06 14:04:31 CST 2015.
 */
package com.ibm.cio.cmr.request.entity;

import java.io.Serializable;

import javax.persistence.Column;
import javax.persistence.Embeddable;

import org.apache.commons.lang.StringUtils;

/**
 * The primary key class for the DELEGATE database table.
 * 
 * @author Jeffrey Zamora
 */
@Embeddable
public class DelegatePK extends BaseEntityPk implements Serializable {

  // default serial version id, required for serializable classes.
  private static final long serialVersionUID = 1L;

  @Column(name = "USER_ID")
  private String userId;

  @Column(name = "DELEGATE_ID")
  private String delegateId;

  public String getUserId() {
    return this.userId;
  }

  public void setUserId(String userId) {
    this.userId = userId;
  }

  public String getDelegateId() {
    return this.delegateId;
  }

  public void setDelegateId(String delegateId) {
    this.delegateId = delegateId;
  }

  public boolean equals(Object other) {
    if (this == other) {
      return true;
    }
    if (!(other instanceof DelegatePK)) {
      return false;
    }
    DelegatePK o = (DelegatePK) other;
    return this.userId.equals(o.userId) && this.delegateId.equals(o.delegateId);

  }

  public int hashCode() {
    final int prime = 31;
    int hash = 17;
    hash = hash * prime + (this.userId != null ? this.userId.hashCode() : 0);
    hash = hash * prime + (this.delegateId != null ? this.delegateId.hashCode() : 0);

    return hash;
  }

  @Override
  protected boolean allKeysAssigned() {
    return !StringUtils.isEmpty(this.userId) && !StringUtils.isEmpty(this.delegateId);

  }
}